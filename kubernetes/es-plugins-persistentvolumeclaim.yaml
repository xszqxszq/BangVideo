apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: es-plugins
  name: es-plugins
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
